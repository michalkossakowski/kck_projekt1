<UserControl x:Class="kck_projekt2.LoginPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"      
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             Foreground="White"
             >

    <Grid>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Name="errorMessage" Content="" Foreground="Red" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <materialDesign:Card Name="Loading" Visibility="Collapsed" Padding="4" UniformCornerRadius="14" Width="28">
                <ProgressBar IsIndeterminate="True" Style="{StaticResource MaterialDesignCircularProgressBar}" Value="50" />
            </materialDesign:Card>
            
            <TextBox Name="nick" materialDesign:HintAssist.HelperText="Enter your name"  materialDesign:HintAssist.Hint="Name" Width="200" Height="30" Margin="0,20,0,0" />
            <PasswordBox Name="password" materialDesign:HintAssist.HelperText="Enter your password"  materialDesign:HintAssist.Hint="Password" materialDesign:TextFieldAssist.HasClearButton="True" Width="200" Height="30"  Margin="0,20,0,0" />
            <Button Content="Login" Width="80" Height="30" Margin="0,20,0,0" Click="LoginClick"/>
            <Button Content="Back" Width="70" Height="30" Margin="0,20,0,0" Click="BackClick" Background="Orange"/>
        </StackPanel>
    </Grid>

</UserControl>